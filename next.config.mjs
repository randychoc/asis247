/** @type {import('next').NextConfig} */
// const nextConfig = {}
const nextConfig = {
  output: 'export',
  basePath: '/asis247',
  images: {
    unoptimized: true,
  },
};

export default nextConfig
